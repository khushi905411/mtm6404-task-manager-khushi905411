const Header = ({ title }) => (
  <header style={{
    backgroundColor: "#EA2F14",
    color: "white",
    textAlign: "center",
    padding: "1rem"
  }}>
    <h1>{title}</h1>
  </header>
);

export default Header;
